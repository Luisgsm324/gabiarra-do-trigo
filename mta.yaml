_schema-version: 3.3.0
ID: gabiarra-do-trigo
description: A simple CAP project.
version: 1.0.0
modules:
  - name: gabiarra-do-trigo-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: gabiarra-do-trigo-auth
      - name: gabiarra-do-trigo-db
      - name: gabiarra-do-trigo-destination
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: gabiarra-do-trigo-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: gabiarra-do-trigo-db
    parameters:
      buildpack: nodejs_buildpack
  - name: gabiarra-do-trigo-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: gabiarra-do-trigo-html5-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: app/
      requires:
        - artifacts:
            - gabiarradotrigo.zip
          name: gabiarradotrigo
          target-path: app/
  - name: gabiarra-do-trigo-destinations
    type: com.sap.application.content
    requires:
      - name: gabiarra-do-trigo-auth
        parameters:
          service-key:
            name: gabiarra-do-trigo-auth-key
      - name: gabiarra-do-trigo-html5-repo-host
        parameters:
          service-key:
            name: gabiarra-do-trigo-html5-repo-host-key
      - name: srv-api
      - name: gabiarra-do-trigo-destination
        parameters:
          content-target: true
    parameters:
      content:
        instance:
          destinations:
            - Name: gabiarra-do-trigo-html5-repository
              ServiceInstanceName: gabiarra-do-trigo-html5-repo-host
              ServiceKeyName: gabiarra-do-trigo-html5-repo-host-key
              sap.cloud.service: gabiarradotrigo.service
            - Authentication: OAuth2UserTokenExchange
              Name: gabiarra-do-trigo-auth
              ServiceInstanceName: gabiarra-do-trigo-auth
              ServiceKeyName: gabiarra-do-trigo-auth-key
              sap.cloud.service: gabiarradotrigo.service
          existing_destinations_policy: update
    build-parameters:
      no-source: true
  - name: gabiarradotrigo
    type: html5
    path: app/gabiarra-do-trigo
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
        - npm ci
      supported-platforms:
        []

  - name: gabiarra-do-trigo
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: gabiarra-do-trigo-auth
      - name: gabiarra-do-trigo-destination
      - name: gabiarra-do-trigo-html5-runtime
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}
resources:
  - name: gabiarra-do-trigo-auth
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        tenant-mode: dedicated
        xsappname: gabiarra-do-trigo-${org}-${space}
        oauth2-configuration:
          redirect-uris:
            - https://*~{app-api/app-uri}/**
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
    requires:
      - name: app-api
  - name: gabiarra-do-trigo-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: gabiarra-do-trigo-destination
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                HTML5.DynamicDestination: true
                HTML5.ForwardAuthToken: true
                Name: srv-api
                ProxyType: Internet
                Type: HTTP
                URL: ~{srv-api/srv-url}
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
      service: destination
      service-plan: lite
    requires:
      - name: srv-api
        group: destinations
        properties:
          forwardAuthToken: true
          name: srv-api
          url: ~{srv-url}
  - name: gabiarra-do-trigo-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: gabiarra-do-trigo-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
